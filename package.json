{
  "name": "js13k-2025-the-soldier",
  "version": "0.0.0",
  "description": "2025 js13kGames entry",
  "main": "index.html",
  "scripts": {
    "serve": "cd dist && http-server -c-1 -p3000",
    "build": "yarn test && ./build",
    "build:watch": "yarn build; while inotifywait -r -e create -e modify -e move -e delete .; do echo; yarn build; echo; done",
    "test": "node --test *.test.js"
  },
  "author": "Aurelio Heckert",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "http-server": "^14.1.1",
    "minify": "^14.1.0",
    "terser": "^5.43.1"
  },
  "engines": {
    "node": "24"
  }
}
