{
  "name": "js13k-2025-the-soldier",
  "version": "0.0.0",
  "description": "2025 js13kGames entry",
  "main": "index.html",
  "scripts": {
    "serve": "cd dist && http-server -c-1 -p3000",
    "build": "./build",
    "build:watch": "npm run -s build; while inotifywait -r -e create -e modify -e move -e delete .; do echo; ./build; echo; done"
  },
  "author": "Aurelio Heckert",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "http-server": "^14.1.1",
    "minify": "^14.1.0",
    "terser": "^5.43.1"
  }
}
